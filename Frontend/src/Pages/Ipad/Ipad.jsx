import React from "react";
import styled from "styled-components";
import Header from "./Header";

const Ipad = () => {
  return (
    <IpadMain>
      <Header />
    </IpadMain>
  );
};

export default Ipad;
// STYLED CSS
const IpadMain = styled.div``;
