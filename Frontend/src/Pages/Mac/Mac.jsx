import React from "react";
import styled from "styled-components";
import Header from "./Header";

const Mac = () => {
  return (
    <MacMain>
      <Header />
    </MacMain>
  );
};

export default Mac;
// STYLED CSS
const MacMain = styled.div``;
